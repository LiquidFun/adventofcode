console.log(
  require("fs")
  .readFileSync(0, "utf8")
  .trim()
  .split("\n\n")
  .at(-1)
  .replaceAll("x", "/9*")
  .replaceAll(": ", ">=")
  .replaceAll(" ", "+")
  .split("\n")
  .filter(x => eval(x) > 0)
  .length)
